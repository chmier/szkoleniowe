package GUI;

public class Szczeniak extends Pies{


    public Szczeniak(int lives, int xAxis, int yAxis, String imagePath, boolean isAlive) {
        super(lives, xAxis, yAxis, imagePath, isAlive);
    }
}
